sudo: required
services:
  - docker

before_install:
  - docker build -t yrios/react-test -f ./client/Dockerfile.dev .

script:
  - docker run yrios/react-test npm test -- --coverage

after_seccess:
  - docker build -t yrios/multi-client ./client
  - docker build -t yrios/multi-nginx ./nginx
  - docker build -t yrios/multi-server ./server
  - docker build -t yrios/multi-worker ./worker
# login
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin

  - docker push yrios/multi-client
  - docker push yrios/multi-nginx
  - docker push yrios/multi-server
  - docker push yrios/multi-worker
